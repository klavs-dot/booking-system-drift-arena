# Drift Arena — Rezervāciju sistēma

Next.js web app ar Google Sheets kā datubāzi.

## Uzstādīšana

1. `npm install`
2. Izveido `.env.local` no `.env.example`
3. Pievieno Google Service Account JSON
4. `npm run dev`

## Deploy

Savienots ar Vercel — katrs push uz `main` automātiski deploy.
